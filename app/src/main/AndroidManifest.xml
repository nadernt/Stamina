<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fleecast.stamina">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name="com.fleecast.stamina.chathead.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.fleecast.stamina.chathead.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fleecast.stamina.launcher.LauncherDialogActivity"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustPan">

            <!--
        <intent-filter>
            <category android:name="android.intent.category.HOME" />
            <category android:name="android.intent.category.APP_BROWSER" />
        </intent-filter>
            -->

        </activity>
        <activity
            android:name="com.fleecast.stamina.launcher.AddEditGroupItem"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name="com.fleecast.stamina.launcher.IconChooserActivity" />
        <activity android:name="com.fleecast.stamina.notetaking.EditActivity" />
        <activity
            android:name="com.fleecast.stamina.notetaking.AddActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name="com.fleecast.stamina.notetaking.NoteTakingRecyclerViewActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.fleecast.stamina.notetaking.Player"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@style/AppTheme.PopPlayer" />

        <service android:name="com.fleecast.stamina.chathead.ChatHeadService" />
        <service android:name="com.fleecast.stamina.notetaking.ChatHeadRecordService" />

        <receiver
            android:name="com.fleecast.stamina.chathead.DeviceAdminSampleReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />

            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <activity android:name="com.fleecast.stamina.notetaking.ActivityNoteTakingSettings" />

        <!--This part is inside the application-->
        <receiver android:name="com.fleecast.stamina.notetaking.PhonecallReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.fleecast.stamina.notetaking.BlackListManagerActivity"
            android:label="@string/title_activity_black_list_manager"
            android:theme="@style/AppTheme"></activity>
        <activity android:name="com.fleecast.stamina.notetaking.ActivityPhoneCallSettings" />
    </application>

</manifest>
